server:
  port: 8080

spring:
  application:
    name: ringle-class-booking-service
  jpa:
    hibernate:
      ddl-auto: none

    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    defer-datasource-initialization: true

  datasource:
    username: root
    password: a01084204241
    url: jdbc:mysql://localhost:3306/ringle?serverTimezone=UTC&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
